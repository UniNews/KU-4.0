import { connect } from 'react-redux';
import HottestView from './HottestView';

const mapStateToProps = state => {
    return {
    }
};

const mapDispatchToProps = {

};

export default connect(mapStateToProps, mapDispatchToProps)(HottestView);