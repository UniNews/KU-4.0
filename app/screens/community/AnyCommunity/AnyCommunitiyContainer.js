import { connect } from 'react-redux';
import LatestView from './AnyCommunityView';

const mapStateToProps = state => {
    return {
    }
};

const mapDispatchToProps = {

};

export default connect(mapStateToProps, mapDispatchToProps)(LatestView);